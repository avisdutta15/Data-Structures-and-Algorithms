| Algorithmic Pattern | Core Concept | Problems in this Category | Exceptions & Notes | Pseudocode Template &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; |
| :--- | :--- | :--- | :--- | :--- |
| **1. The "Include / Exclude" Pattern** | 0/1 Knapsack style. A binary decision tree where you simply choose whether to pick an element or skip it at the current index. | • Print all/one/count Sequences With Sum = K<br>• Strings by placing spaces<br>• Max Number of Achievable Transfer Requests<br>• Max Score Words Formed by Letters<br>• **Max Length of a Concatenated String with Unique Characters** | Pure binary recursion. *Max Length Concatenated String* uses bitmasking to quickly check for duplicate characters before including a string. | <code>function backtrack(index, path):<br>&nbsp;&nbsp;if base_condition:<br>&nbsp;&nbsp;&nbsp;&nbsp;save path; return<br>&nbsp;&nbsp;<b>// Include</b><br>&nbsp;&nbsp;path.push(arr[index])<br>&nbsp;&nbsp;backtrack(index + 1, path)<br>&nbsp;&nbsp;path.pop()<br>&nbsp;&nbsp;<b>// Exclude</b><br>&nbsp;&nbsp;backtrack(index + 1, path)</code> |
| **2. Combinations & Subsets** | For-loop backtracking. Order does *not* matter. Focuses on deduplication and pruning valid combinations. | • Subsets I, II<br>• Combinations<br>• Combination Sum I, II, III, IV<br>• Non-decreasing Subsequences<br>• The Number of Beautiful Subsets | **Comb Sum IV** is DP. **Non-decreasing Subsequences** requires a local `unordered_set` for deduplication instead of `arr[i] == arr[i-1]`. | <code>function backtrack(startIndex, path):<br>&nbsp;&nbsp;save path to result<br>&nbsp;&nbsp;for i = startIndex to n-1:<br>&nbsp;&nbsp;&nbsp;&nbsp;<b>// Deduplicate</b><br>&nbsp;&nbsp;&nbsp;&nbsp;if i > startIndex and arr[i] == arr[i-1]: continue<br>&nbsp;&nbsp;&nbsp;&nbsp;path.push(arr[i])<br>&nbsp;&nbsp;&nbsp;&nbsp;backtrack(i + 1, path) <b>// i+1 prevents reuse</b><br>&nbsp;&nbsp;&nbsp;&nbsp;path.pop()</code> |
| **3. Permutations** | Order matters. Uses a `visited` boolean array or in-place swapping to track elements across recursive depths. | • Permutation I, II<br>• Palindrome Permutation I, II<br>• Letter Case Permutation<br>• Next Permutation<br>• Find the Minimum Cost Array Permutation<br>• Letter Tile Possibilities | **Next Permutation** is an array manipulation/math problem, not backtracking. | <code>function backtrack(path, visited):<br>&nbsp;&nbsp;if path.size == n:<br>&nbsp;&nbsp;&nbsp;&nbsp;save path; return<br>&nbsp;&nbsp;for i = 0 to n-1:<br>&nbsp;&nbsp;&nbsp;&nbsp;if visited[i]: continue<br>&nbsp;&nbsp;&nbsp;&nbsp;<b>// Deduplicate</b><br>&nbsp;&nbsp;&nbsp;&nbsp;if i > 0 and arr[i] == arr[i-1] and not visited[i-1]: continue<br>&nbsp;&nbsp;&nbsp;&nbsp;visited[i] = true<br>&nbsp;&nbsp;&nbsp;&nbsp;path.push(arr[i])<br>&nbsp;&nbsp;&nbsp;&nbsp;backtrack(path, visited)<br>&nbsp;&nbsp;&nbsp;&nbsp;path.pop()<br>&nbsp;&nbsp;&nbsp;&nbsp;visited[i] = false</code> |
| **4. 2D Grid DFS & Pathfinding** | Matrix traversal (Up, Down, Left, Right). Requires boundary checks and temporarily marking cells as `visited`. | • Word Search I, II<br>• Check if Word Can Be Placed In Crossword <br>• Unique Path I, II, III<br>• Rat In a Maze<br>• Longest/Shortest Path in Maze<br>• Print All paths<br>• Path with Maximum Gold | **Word Search II** combines DFS with a Trie. **Unique Paths I & II** are DP problems. | <code>function dfs(r, c, path):<br>&nbsp;&nbsp;if out_of_bounds(r, c) or visited(r, c): return<br>&nbsp;&nbsp;if target_reached(r, c): save path; return<br>&nbsp;&nbsp;visited(r, c) = true<br>&nbsp;&nbsp;path.push(cell)<br>&nbsp;&nbsp;for dir in [(0,1), (1,0), (0,-1), (-1,0)]:<br>&nbsp;&nbsp;&nbsp;&nbsp;dfs(r + dir.x, c + dir.y, path)<br>&nbsp;&nbsp;path.pop()<br>&nbsp;&nbsp;visited(r, c) = false <b>// backtrack</b></code> |
| **5. String Partitioning** | Uses a `for` loop to slice strings into prefixes/suffixes. Checks if the prefix is valid before recursing. | • Palindrome Partitioning I, II<br>• Word Break I, II<br>• Word Pattern I, II<br>• Restore IP Addresses<br>• Split a String Into Max Unique Substrings<br>• Pseudo-Palindromic Paths in a Tree <br>• **Splitting a String Into Descending Consecutive Values** | **Word Break I** & **Pal Part II** are DP. *Descending Consecutive Values* compares the sliced integer to `prev - 1` before recursing. | <code>function backtrack(startIndex, path, str):<br>&nbsp;&nbsp;if startIndex == str.length:<br>&nbsp;&nbsp;&nbsp;&nbsp;save path; return<br>&nbsp;&nbsp;for end = startIndex to str.length - 1:<br>&nbsp;&nbsp;&nbsp;&nbsp;substring = str[startIndex...end]<br>&nbsp;&nbsp;&nbsp;&nbsp;if is_valid(substring):<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;path.push(substring)<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;backtrack(end + 1, path, str)<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;path.pop()</code> |
| **6. Constraint Satisfaction** | Heavy backtracking. Requires dedicated helper functions (like `isSafe()`) to validate complex rules before placing an item. | • Sudoku Solver<br>• N-Queens I, II<br>• Hamiltonian Circuit<br>• 24 Game<br>• **Partition to K Equal Sum Subsets**<br>• **Matchsticks to Square**<br>• **M-Coloring Problem** | *Matchsticks to Square* is just *K Equal Sum Subsets* hardcoded to `K=4`. Both require aggressive pruning (sorting descending, skipping identical failed buckets) to avoid TLE. | <code>function solve(board):<br>&nbsp;&nbsp;if board_is_full: return true<br>&nbsp;&nbsp;cell = get_next_empty_cell(board)<br>&nbsp;&nbsp;for candidate in possible_values:<br>&nbsp;&nbsp;&nbsp;&nbsp;if is_safe(board, cell, candidate):<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;place(candidate, cell)<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;if solve(board): return true<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;remove(candidate, cell) <b>// backtrack</b><br>&nbsp;&nbsp;return false</code> |
| **7. Choice Trees** | Multi-branch generation mapping choices between different states, buckets, or characters. | • Letter Combinations of a Phone Number<br>• Generate Parentheses<br> • Remove Invalid Parentheses <br>• Fair Distribution of Cookies<br>• **Find Unique Binary String** | *Find Unique Binary String* can be solved via recursive branching (0 or 1), but optimal $O(N)$ solution uses **Cantor's Diagonalization** (flip the $i$-th bit of the $i$-th string). | <code>function backtrack(index, path):<br>&nbsp;&nbsp;if index == total_groups:<br>&nbsp;&nbsp;&nbsp;&nbsp;save path; return<br>&nbsp;&nbsp;for choice in group[index]:<br>&nbsp;&nbsp;&nbsp;&nbsp;path.push(choice)<br>&nbsp;&nbsp;&nbsp;&nbsp;backtrack(index + 1, path)<br>&nbsp;&nbsp;&nbsp;&nbsp;path.pop()</code> |
| **8. Pure DP, Graph & Math** | Problems that look like recursion but will fail without a 2D DP table, Graph BFS/DFS, or Math. | • Regular Expression Matching<br>• Wildcard Matching<br>• Word Ladder I, II<br>• **Reconstruct Itinerary**<br>• **Kth Permutation Sequence** | *Reconstruct Itinerary* requires **Hierholzer's Algorithm** (Eulerian Path) using post-order DFS to avoid dead ends. *Kth Permutation* uses **Factorial Math** to find the exact sequence in $O(N^2)$ time instead of $O(N!)$ generation. | <code><b>// DP / Memoization template</b><br>function solve(i, j, memo):<br>&nbsp;&nbsp;if base_case: return value<br>&nbsp;&nbsp;if (i, j) in memo: return memo[(i, j)]<br>&nbsp;&nbsp;ans = calculate_transitions()<br>&nbsp;&nbsp;memo[(i, j)] = ans<br>&nbsp;&nbsp;return ans</code> |