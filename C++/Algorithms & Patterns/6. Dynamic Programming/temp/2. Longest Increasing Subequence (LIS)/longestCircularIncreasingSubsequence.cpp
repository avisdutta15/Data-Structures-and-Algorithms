#include <bits/stdc++.h>
using namespace std;

/*
    Append the same elements(i.e. whole array) with the given array.
    For every window of size n(no. of elements in the given array), perform LIS.
    Return maximum length.
*/


class Solution{
    public:
        int longestCircularIncreasingSubsequence(vector<int> A){
            
        }
};

int main(){
    vector<int> A = {5, 4, 3, 2, 1};
    Solution obj;
    cout<<obj.longestCircularIncreasingSubsequence(A);
    return 0;
}