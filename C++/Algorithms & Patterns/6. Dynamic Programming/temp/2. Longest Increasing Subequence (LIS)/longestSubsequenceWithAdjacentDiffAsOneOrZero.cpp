#include <bits/stdc++.h>
using namespace std;

/*
    Given an array of n integers. The problem is to find maximum length of the subsequence with 
    difference between adjacent elements in the subsequence as either 0 or 1.

    Input : arr[] = {2, 5, 6, 3, 7, 6, 5, 8}
    Output : 5
    The subsequence is {5, 6, 7, 6, 5}.
*/